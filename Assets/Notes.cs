/*

	UNITY 2018.2.18f1
	TODO:
	
	Object Pool Data Structure (40pts)
You will implement an object pool as described in the slides (generating a bunch of objects ahead of time, and then managing them). You may 
choose to use the supplementary readings for inspiration on implementing the object pool (which I would highly recommend).

    1. (2.5pts) Object Pool Capacity: If the Object Pool does not have enough objects in its pool, it should return null. It should also have
	 a method where you can specify a number to increase the capacity of the pool e.g. public virtual void increasePool(int capacity);

*/
